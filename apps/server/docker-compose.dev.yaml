# WIP WIP WIP
services:
  mariadb:
    image: mariadb:11.4
    container_name: loveline-mariadb
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - "3307:3306"
    volumes:
      - mariadb-data:/var/lib/mysql
    networks:
      - loveline-net

  server:
    build: .
    container_name: loveline-server
    environment:
      DATABASE_HOST: mariadb
      DATABASE_PORT: 3306
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    depends_on:
      - mariadb
    ports:
      - "8082:8080"
    volumes:
      - ./src:/usr/src/app/src # Mount source code for development
    networks:
      - loveline-net

volumes:
  mariadb-data:

networks:
  loveline-net:
